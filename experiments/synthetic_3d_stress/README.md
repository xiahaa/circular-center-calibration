# Synthetic 3D Stress

Experiment-owned implementation of the angular-support sweep in paper Figure 6.
Use `profiles/ci.yaml` for wiring checks and `profiles/paper.yaml` for the published
grid. User-facing commands and output checks are documented under
`docs/experiments/synthetic_3d_stress/`.
